


## 20260310

1つのプロンプトに対して、複数のAIプロバイダー（’１個のプロバイダーに対してN個の複数のモデル）にAPI実行して結果を表示するWebページを作って。
- GithubPagesで公開する(データ保持等はできない、処理はユーザーサイド）
- モデル名・APIキーの２つは変数（ユーザー設定可能）、AIプロバイダーは固定
- APIキーおよびモデル名は、ローカル実行時は.envファイル（コミットしない）から読み込み。公開ページには「キー登録（BYOK）」ボタンから登録できるようにする。
- 入力されたキーはブラウザの localStorage に保存し、自分（開発者）に送られないようにする。
- gitignoreを適切に設定すること
- API実行がエラーで返ってきた場合はエラー詳細を秘匿せず、長くなりすぎない範囲で表示
- API呼び出し方と、対象のAIプロバイダーはこのプログラムを参考にして。複数モデル・複数APIキーへの対応要件も引き継ぐこと
C:\Users\user\Documents\PGM連携\battle_board\docs\poc\free_ai_api\ai_api_poc.py
- envファイルはこれをそのまま使用できるように実装
C:\Users\user\Documents\PGM連携\battle_board\docs\poc\free_ai_api\.env.local
- レイアウトに関する要件：①各AIプロバイダーの公式アイコン画像を探して設定。②画面上部にプロンプト欄。結果出力欄は横２列のカード型レイアウトで並べる（立幅は可変でなく固定、複数行が入る程度に。スクロールバーつき）。③実行中のステータスを各カードの背景色で示す（出力結果がエラーになったら赤系にする等）

- 今回のページの出力先はここ。
C:\Users\user\Documents\PGM連携\100challenge\20260310


### フィードバック
ビルド時にAPIキーが埋め込まれる問題を避けるためにビルドモードの分岐を入れる必要があった


